# Core
pydantic>=2.6
pyyaml>=6.0
httpx>=0.27
pyjwt>=2.8

# Agent brain (orchestration)
langgraph>=0.2

# LLM providers (optional at runtime; pick one via scope.llm_provider)
anthropic>=0.40
# openai>=1.0          # uncomment if using OpenAI or Cerebras (OpenAI-compatible)

# Fixture app + tests
fastapi>=0.110
uvicorn>=0.27
pytest>=8.0
